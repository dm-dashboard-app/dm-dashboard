import React, { useState, useEffect, useRef } from 'react';
import { supabase } from '../supabaseClient';
import { DmgHealRow } from './PlayerCard';
import { getUnifiedResourceConfig, readBooleanField, readNumberField, getClassEntries } from '../utils/classResources';

const CONDITIONS = [
  { code: 'BLD', label: 'Blinded', colour: '#6b5b95' }, { code: 'CHM', label: 'Charmed', colour: '#e8a0bf' }, { code: 'DEF', label: 'Deafened', colour: '#888' }, { code: 'FRG', label: 'Frightened', colour: '#9b2335' }, { code: 'GRP', label: 'Grappled', colour: '#8b6914' }, { code: 'INC', label: 'Incapacitated', colour: '#b5451b' }, { code: 'INV', label: 'Invisible', colour: '#4a4a7a' }, { code: 'PAR', label: 'Paralyzed', colour: '#7a4a9a' }, { code: 'PET', label: 'Petrified', colour: '#888' }, { code: 'PSN', label: 'Poisoned', colour: '#3a7a3a' }, { code: 'PRN', label: 'Prone', colour: '#7a6030' }, { code: 'RST', label: 'Restrained', colour: '#8b4513' }, { code: 'STN', label: 'Stunned', colour: '#4a4a7a' }, { code: 'UNC', label: 'Unconscious', colour: '#2a2a3a' }, { code: 'EXH', label: 'Exhaustion', colour: '#5a3a7a' }, { code: 'HEX', label: 'Hex', colour: '#6a0dad' },
];
const CONDITION_COLOURS = Object.fromEntries(CONDITIONS.map(c => [c.code, c.colour]));
const DAMAGE_TYPES = ['acid','bludgeoning','cold','fire','force','lightning','necrotic','piercing','poison','psychic','radiant','slashing','thunder'];
const MODS = ['str','dex','con','int','wis','cha'];
function sortCombatants(combatants) { return [...combatants].sort((a,b)=>{ const ai=a.initiative_total ?? -999; const bi=b.initiative_total ?? -999; if (bi!==ai) return bi-ai; const am=a.initiative_mod ?? 0; const bm=b.initiative_mod ?? 0; if (bm!==am) return bm-am; return a.id < b.id ? -1 : 1; }); }
export function getDisplayOrderedCombatants(combatants, turnIndex) { if (!combatants.length) return []; const safeIndex=Math.max(0, Math.min(turnIndex ?? 0, combatants.length-1)); return [...combatants.slice(safeIndex), ...combatants.slice(0,safeIndex)]; }
function logCombat(encounterId, actor, action, detail) { if (!encounterId) return; supabase.from('combat_log').insert({ encounter_id: encounterId, actor, action, detail }).then(()=>{}); }
function nextZeroHpConditions(newHp, conditions = []) { const next=[...conditions]; if (newHp===0) { if (!next.includes('UNC')) next.push('UNC'); if (!next.includes('PRN')) next.push('PRN'); return next; } if (newHp>0) return next.filter(c=>c!=='UNC'); return next; }
function compactObject(obj){ return Object.fromEntries(Object.entries(obj).filter(([,v])=>v!==undefined)); }
function formatSingleClassEntry(entry){ if(!entry) return ''; const classPart=[entry.displayClass, entry.subclassName].filter(Boolean).join(' • '); const levelPart=entry.level ? `Level ${entry.level}` : ''; return [classPart, levelPart].filter(Boolean).join(' • '); }
function formatClassLine(source = {}) { return getClassEntries(source).map(formatSingleClassEntry).filter(Boolean).join(' / '); }
function getResourceConfig(profile = {}, state = {}) { return getUnifiedResourceConfig(profile, state, { compactLabels: true, includeHitDice: true, includeWarlockSlots: true, includeNaturalRecovery: false }); }
function MiniHpBar({ current, max, tempHp = 0, color, label }) { const pct=max>0?Math.max(0,Math.min(100,(current/max)*100)):0; const tempPct=max>0?Math.max(0,Math.min(100,(tempHp/max)*100)):0; const barColor=color || (pct>50?'var(--hp-high)':pct>25?'var(--hp-mid)':'var(--hp-low)'); return <div style={{display:'flex',flexDirection:'column',gap:2}}>{label && <span style={{fontSize:9,color:'var(--text-muted)',textTransform:'uppercase',letterSpacing:'0.04em'}}>{label}</span>}<div style={{position:'relative',height:6,background:'var(--bg-panel-3)',borderRadius:3,overflow:'hidden'}}><div style={{position:'absolute',left:0,top:0,height:'100%',width:`${pct}%`,background:barColor,borderRadius:3,transition:'width 0.3s'}} />{tempHp>0 && <div style={{position:'absolute',right:0,top:0,height:'100%',width:`${Math.min(tempPct,100-pct)}%`,background:'var(--hp-temp)',opacity:0.7,borderRadius:3}} />}</div></div>; }
function LegendaryPips({ label, max, used, isDM, onSpend, onRestore, onReset, isActive }) { if (max===0) return null; return <div style={{display:'flex',alignItems:'center',gap:4}}><span style={{fontSize:9,color:'var(--accent-gold)',fontWeight:700,width:16,textTransform:'uppercase'}}>{label}</span>{Array.from({length:max}).map((_,i)=>{ const isSpent=i<used; return <button key={i} onClick={e=>{e.stopPropagation(); if(!isDM) return; isSpent?onRestore():onSpend();}} style={{width:14,height:14,borderRadius:'50%',border:`2px solid var(--accent-gold)`,background:isSpent?'transparent':'var(--accent-gold)',cursor:isDM?'pointer':'default',padding:0}} />; })}<span style={{fontSize:9,color:'var(--text-muted)',marginLeft:2}}>{used>0?`${max-used}/${max}`:`${max}/${max}`}</span>{isDM && isActive && <button onClick={e=>{e.stopPropagation(); onReset();}} style={{fontSize:11,color:'var(--accent-gold)',opacity:used===0?0.2:0.7,padding:'0 2px',cursor:used===0?'default':'pointer',background:'none',border:'none'}} disabled={used===0}>↺</button>}</div>; }
function EnemySlotGrid({ combatant, onUpdate }) { const levels=[1,2,3,4,5,6,7,8,9]; const activeLevels=levels.filter(l=>(combatant[`slots_max_${l}`] || 0)>0); if(activeLevels.length===0) return null; async function useSlot(level){ const used=combatant[`slots_used_${level}`]||0; const max=combatant[`slots_max_${level}`]||0; if(used>=max) return; await supabase.from('combatants').update({[`slots_used_${level}`]:used+1}).eq('id', combatant.id); onUpdate(); } async function restoreSlot(level){ const used=combatant[`slots_used_${level}`]||0; if(used<=0) return; await supabase.from('combatants').update({[`slots_used_${level}`]:used-1}).eq('id', combatant.id); onUpdate(); } async function resetLevel(level){ await supabase.from('combatants').update({[`slots_used_${level}`]:0}).eq('id', combatant.id); onUpdate(); } return <div style={{marginBottom:8}}><div style={{fontSize:10,color:'var(--text-secondary)',fontWeight:700,marginBottom:4,textTransform:'uppercase',letterSpacing:'0.05em'}}>Spell Slots</div><div className="slots-grid">{activeLevels.map(level=>{ const max=combatant[`slots_max_${level}`]||0; const used=combatant[`slots_used_${level}`]||0; const allUsed=used>=max; return <div key={level} className="slots-row"><span className="slots-label">{level}</span>{Array.from({length:max}).map((_,i)=>{ const isUsed=i<used; return <button key={i} className={`slot-pip ${isUsed?'used':'available'}`} onClick={()=>isUsed?restoreSlot(level):useSlot(level)} style={{cursor:'pointer'}} />; })}{used>0 && <button className="slots-reset-btn" onClick={()=>resetLevel(level)}>↺</button>}{allUsed && <span className="slots-empty-label">expended</span>}</div>; })}</div></div>; }
function PcResourceSummary({ profile, state, isDM, onUpdate }) { const resources = getResourceConfig(profile || {}, state || {}).filter(resource => !['natural-recovery'].includes(resource.id)); if(resources.length===0) return null; async function updateResourceFields(updates){ const payload=compactObject(updates); if(!state?.id || Object.keys(payload).length===0) return; await supabase.from('player_encounter_state').update(payload).eq('id', state.id); onUpdate(); } return <div style={{marginTop:6,display:'flex',flexWrap:'wrap',gap:6}}>{resources.map(resource=><PcResourceChip key={resource.id} resource={resource} state={state} isDM={isDM} onUpdateFields={updateResourceFields} />)}</div>; }
function PcResourceChip({ resource, state, isDM, onUpdateFields }) { if(resource.type==='toggle'){ const rawValue=readBooleanField(state,[resource.boolKey],false); const value=(resource.id==='second-wind' || resource.id==='arcane-recovery') ? !rawValue : rawValue; async function toggle(){ if(!isDM) return; const nextStored=(resource.id==='second-wind' || resource.id==='arcane-recovery') ? value : !value; await onUpdateFields({[resource.boolKey]:nextStored}); } return <button type="button" onClick={toggle} disabled={!isDM} style={{display:'inline-flex',alignItems:'center',gap:4,padding:'2px 7px',borderRadius:999,border:`1px solid ${value?'var(--accent-green)':'var(--accent-red)'}`,background:value?'rgba(62,207,106,0.08)':'rgba(224,48,80,0.08)',color:value?'var(--accent-green)':'var(--accent-red)',fontSize:10,fontWeight:700,cursor:isDM?'pointer':'default'}}><span>{resource.label}</span><span>{value ? (resource.falseLabel || 'Ready') : (resource.trueLabel || 'Used')}</span>{resource.meta ? <span style={{color:'var(--text-muted)'}}>• {resource.meta}</span> : null}</button>; }
 const current=readNumberField(state,[resource.currentKey],0); const max=readNumberField(state,[resource.maxKey],null); if(resource.type==='counter'){ async function adjust(delta){ if(!isDM) return; const upper=max ?? Math.max(current,0); const next=Math.max(0,Math.min(upper,current+delta)); if(next===current) return; await onUpdateFields({[resource.currentKey]:next}); } return <div style={{display:'inline-flex',alignItems:'center',gap:4,padding:'2px 7px',borderRadius:999,border:'1px solid var(--border)',background:'var(--bg-panel-3)',fontSize:10,fontWeight:700}}><span>{resource.label}</span>{isDM && <button onClick={()=>adjust(-1)} style={{background:'none',border:'none',color:'var(--text-secondary)',cursor:'pointer',padding:0}}>−</button>}<span>{current}{max!==null?`/${max}`:''}</span>{resource.meta ? <span style={{color:'var(--text-muted)'}}>{resource.meta}</span> : null}{isDM && <button onClick={()=>adjust(1)} style={{background:'none',border:'none',color:'var(--text-secondary)',cursor:'pointer',padding:0}}>+</button>}</div>; }
 const safeMax=Math.max(0,max ?? current ?? 0); const safeCurrent=Math.max(0,Math.min(safeMax,current ?? 0)); async function setPips(nextCurrent){ if(!isDM) return; const clamped=Math.max(0,Math.min(safeMax,nextCurrent)); await onUpdateFields({[resource.currentKey]:clamped}); } return <div style={{display:'inline-flex',alignItems:'center',gap:4,padding:'2px 7px',borderRadius:999,border:'1px solid var(--border)',background:'var(--bg-panel-3)',fontSize:10,fontWeight:700}}><span>{resource.label}</span><span style={{display:'inline-flex',gap:3}}>{Array.from({length:safeMax}).map((_,i)=>{ const active=i<safeCurrent; return <button key={i} type="button" onClick={()=>setPips(active?i:i+1)} disabled={!isDM} style={{width:10,height:10,borderRadius:'50%',padding:0,border:`1.5px solid ${active?'var(--accent-blue)':'var(--border-strong)'}`,background:active?'var(--accent-blue)':'transparent',cursor:isDM?'pointer':'default'}} />; })}</span>{resource.meta ? <span style={{color:'var(--text-muted)'}}>{resource.meta}</span> : null}</div>; }

export default function InitiativePanel({ encounter, combatants, playerStates = [], role, onUpdate, myCombatantId = null }) {
  const isDM = role === 'dm'; const isDisplay = role === 'display'; const sortedOriginal = sortCombatants(combatants); const activeTurnIndex = encounter?.turn_index ?? 0; const displayOrdered = getDisplayOrderedCombatants(sortedOriginal, activeTurnIndex); const [showAddCombatant, setShowAddCombatant] = useState(false); const activeRowRef = useRef(null); const lastActiveIdRef = useRef(null); const activeCombatantId = sortedOriginal[activeTurnIndex]?.id ?? null;
  useEffect(()=>{ if(!activeCombatantId || !activeRowRef.current) return; if(lastActiveIdRef.current===activeCombatantId) return; lastActiveIdRef.current=activeCombatantId; activeRowRef.current.scrollIntoView({behavior:'smooth',block:'nearest',inline:'nearest'}); }, [activeCombatantId]);
  return <div className="panel"><div className="panel-title">{isDisplay ? 'Initiative Feed' : 'Initiative Order'}</div><div className="initiative-list">{displayOrdered.length===0 && <div className="empty-state">No combatants yet.</div>}{displayOrdered.map((c, displayIndex)=>{ const playerState = playerStates.find(s=>s.combatant_id===c.id); const originalIndex = sortedOriginal.findIndex(item=>item.id===c.id); const isActive = c.id===activeCombatantId; const isNextUp = displayIndex===1; return <div key={c.id} ref={isActive?activeRowRef:null}><InitiativeRow combatant={c} playerState={playerState} isActive={isActive} isNextUp={isNextUp} isDM={isDM} isDisplay={isDisplay} onUpdate={onUpdate} sorted={sortedOriginal} idx={originalIndex} encounterId={encounter?.id} myCombatantId={myCombatantId} /></div>; })}</div>{isDM && encounter && <div style={{marginTop:12}}><button className="btn btn-ghost" onClick={()=>setShowAddCombatant(true)}>+ Add Combatant</button></div>}{isDM && encounter && showAddCombatant && <AddCombatantModal encounterId={encounter.id} onUpdate={onUpdate} onClose={()=>setShowAddCombatant(false)} />}</div>;
}

function InitiativeRow({ combatant, playerState, isActive, isNextUp, isDM, isDisplay, onUpdate, sorted, idx, encounterId, myCombatantId }) {
  const [expanded, setExpanded] = useState(false); const [condPickerOpen, setCondPickerOpen] = useState(false); const [resPicker, setResPicker] = useState(false); const [inputValue, setInputValue] = useState(combatant.initiative_total != null ? String(combatant.initiative_total) : ''); const [conDc, setConDc] = useState(null); const conTimer = useRef(null); const isFocused = useRef(false); const savingRef = useRef(false); useEffect(()=>{ if(!isFocused.current) setInputValue(combatant.initiative_total != null ? String(combatant.initiative_total) : ''); }, [combatant.initiative_total]); useEffect(()=>()=>clearTimeout(conTimer.current), []);
  const isPC = combatant.side==='PC'; const isNPC=combatant.side==='NPC'; const isEnemy=combatant.side==='ENEMY'; const isNonPC=isNPC||isEnemy; const conditions=combatant.conditions || []; const pcHpCurrent=playerState?.current_hp ?? null; const pcProfileMax=playerState?.profiles_players?.max_hp ?? null; const pcMaxOverride=playerState?.max_hp_override ?? null; const pcHpMax=pcMaxOverride !== null ? pcMaxOverride : pcProfileMax; const tempHp=playerState?.temp_hp ?? 0; const concentration=playerState?.concentration ?? false; const reactionUsed=playerState?.reaction_used ?? false; const pcConditions=playerState?.conditions || []; const displayConditions=isPC ? pcConditions : conditions; const passivePerception=isPC && playerState?.profiles_players ? (10 + (playerState.profiles_players.skill_perception ?? 0)) : null; const wsActive=playerState?.wildshape_active ?? false; const wsHpCurrent=playerState?.wildshape_hp_current ?? 0; const wsFormName=playerState?.wildshape_form_name ?? null; const wsHpMax=playerState?.wildshape_hp_max ?? null; const enemyHpCurrent=combatant.hp_current ?? null; const enemyHpMax=combatant.hp_max ?? null; const enemyReactionUsed=combatant.reaction_used ?? false; const showPcHp=isPC && pcHpCurrent !== null && pcHpMax !== null; const showNonPcHp=isNonPC && enemyHpCurrent !== null && enemyHpMax !== null && (isDM || (isDisplay && isNPC)); const pcBloodied=isPC && pcHpCurrent !== null && pcHpMax !== null && pcHpCurrent>0 && pcHpCurrent <= Math.floor(pcHpMax/2); const enemyBloodied=isNonPC && combatant.public_status==='BLOODIED'; const laMax=combatant.legendary_actions_max ?? 0; const laUsed=combatant.legendary_actions_used ?? 0; const lrMax=combatant.legendary_resistances_max ?? 0; const lrUsed=combatant.legendary_resistances_used ?? 0; const hasLegendary=isNonPC && (laMax>0 || lrMax>0); const resistances=combatant.resistances || []; const immunities=combatant.immunities || []; const hasAnyMod=MODS.some(m=>(combatant[`mod_${m}`] ?? 0)!==0); const rxUsed=isPC ? reactionUsed : enemyReactionUsed; const canToggleReaction=isDM || (!isDM && isPC && myCombatantId && combatant.id===myCombatantId); const pcProfile=playerState?.profiles_players || {}; const classLine=isPC ? formatClassLine(pcProfile) : formatClassLine(combatant); const ancestryLine=isPC ? (pcProfile.ancestry_name || '') : ''; const miniMarker=combatant.mini_marker || ''; const pcResources = isPC ? getResourceConfig(pcProfile, playerState || {}) : [];
  async function saveInitiative(){ if(savingRef.current) return; const total=parseInt(inputValue,10); if(isNaN(total)) return; savingRef.current=true; await supabase.rpc('set_initiative',{p_combatant_id:combatant.id,p_total:total}); savingRef.current=false; onUpdate(); }
  async function moveUp(){ if(idx<=0) return; const neighbor=sorted[idx-1]; await supabase.rpc('set_initiative',{p_combatant_id:combatant.id,p_total:(neighbor.initiative_total ?? 0)+1}); onUpdate(); }
  async function moveDown(){ if(idx===-1 || idx>=sorted.length-1) return; const neighbor=sorted[idx+1]; await supabase.rpc('set_initiative',{p_combatant_id:combatant.id,p_total:Math.max(0,(neighbor.initiative_total ?? 0)-1)}); onUpdate(); }
  async function toggleReaction(){ if(!canToggleReaction) return; if(isPC){ if(!playerState) return; await supabase.from('player_encounter_state').update({reaction_used:!reactionUsed}).eq('id', playerState.id); } else { await supabase.from('combatants').update({reaction_used:!enemyReactionUsed}).eq('id', combatant.id); } onUpdate(); }
  async function togglePcConcentration(){ if(!isDM || !playerState) return; const updates={ concentration: !concentration }; if(concentration) updates.concentration_check_dc=null; await supabase.from('player_encounter_state').update(updates).eq('id', playerState.id); onUpdate(); }
  async function togglePcCondition(code){ if(!isDM || !playerState) return; const current=playerState.conditions || []; const updated=current.includes(code)?current.filter(c=>c!==code):[...current, code]; await supabase.from('player_encounter_state').update({conditions:updated}).eq('id', playerState.id); onUpdate(); }
  async function applyEnemyDamage(amount){ if(!amount || amount<=0) return; const oldHp=combatant.hp_current ?? 0; const newHp=Math.max(0, oldHp-amount); const isBloodiedNow=newHp>0 && newHp <= Math.floor((combatant.hp_max ?? 0)/2); const updatedConditions=nextZeroHpConditions(newHp,conditions); await supabase.from('combatants').update({hp_current:newHp, public_status:isBloodiedNow?'BLOODIED':null, conditions:updatedConditions}).eq('id', combatant.id); logCombat(encounterId,'DM','damage',`${combatant.name}: -${amount} HP (${oldHp} → ${newHp})`); onUpdate(); }
  async function applyEnemyHeal(amount){ if(!amount || amount<=0) return; const oldHp=combatant.hp_current ?? 0; const newHp=Math.min(combatant.hp_max ?? 999, oldHp+amount); const isBloodiedNow=newHp>0 && newHp <= Math.floor((combatant.hp_max ?? 0)/2); const updatedConditions=nextZeroHpConditions(newHp,conditions); await supabase.from('combatants').update({hp_current:newHp, public_status:isBloodiedNow?'BLOODIED':null, conditions:updatedConditions}).eq('id', combatant.id); logCombat(encounterId,'DM','heal',`${combatant.name}: +${amount} HP (${oldHp} → ${newHp})`); onUpdate(); }
  async function applyPcDamage(amount){ if(!playerState || !amount || amount<=0) return; const curHp=playerState.current_hp ?? 0; const curTemp=playerState.temp_hp ?? 0; const isConc=playerState.concentration ?? false; if(isConc){ const dc=Math.max(10, Math.floor(amount/2)); clearTimeout(conTimer.current); setConDc(dc); conTimer.current=setTimeout(()=>setConDc(null),7000); await supabase.from('player_encounter_state').update({concentration_check_dc:dc}).eq('id', playerState.id); supabase.from('concentration_checks').insert({encounter_id:encounterId, player_name:combatant.name, dc}).then(()=>{}); } let remaining=amount; const updates={}; if(curTemp>0){ const burn=Math.min(curTemp,remaining); remaining -= burn; updates.temp_hp=curTemp-burn; } const oldHp=curHp; if(remaining>0){ const newHp=Math.max(0,curHp-remaining); const updatedConditions=nextZeroHpConditions(newHp,pcConditions); updates.current_hp=newHp; updates.conditions=updatedConditions; await supabase.from('player_encounter_state').update(updates).eq('id', playerState.id); logCombat(encounterId,'DM','damage',`${combatant.name}: -${amount} HP (${oldHp} → ${newHp})`); } else { await supabase.from('player_encounter_state').update(updates).eq('id', playerState.id); logCombat(encounterId,'DM','damage',`${combatant.name}: -${amount} (temp HP absorbed)`); } onUpdate(); }
  async function applyPcHeal(amount){ if(!playerState || !amount || amount<=0) return; const curHp=playerState.current_hp ?? 0; const effectiveMax=pcMaxOverride ?? pcProfileMax ?? 999; const newHp=Math.min(effectiveMax,curHp+amount); const updatedConditions=nextZeroHpConditions(newHp,pcConditions); await supabase.from('player_encounter_state').update({current_hp:newHp,conditions:updatedConditions}).eq('id', playerState.id); logCombat(encounterId,'DM','heal',`${combatant.name}: +${amount} HP (${curHp} → ${newHp})`); onUpdate(); }
  async function toggleEnemyCondition(code){ const updated=conditions.includes(code)?conditions.filter(c=>c!==code):[...conditions,code]; await supabase.from('combatants').update({conditions:updated}).eq('id',combatant.id); onUpdate(); }
  async function toggleDamageType(field,type){ const arr=combatant[field] || []; const updated=arr.includes(type)?arr.filter(t=>t!==type):[...arr,type]; await supabase.from('combatants').update({[field]:updated}).eq('id',combatant.id); onUpdate(); }
  async function spendLegendary(field,max){ const cur=combatant[field] ?? 0; if(cur>=max) return; await supabase.from('combatants').update({[field]:cur+1}).eq('id',combatant.id); onUpdate(); }
  async function restoreLegendary(field){ const cur=combatant[field] ?? 0; if(cur<=0) return; await supabase.from('combatants').update({[field]:cur-1}).eq('id',combatant.id); onUpdate(); }
  async function resetLegendary(field){ await supabase.from('combatants').update({[field]:0}).eq('id',combatant.id); onUpdate(); }
  async function removeCombatant(){ if(!window.confirm(`Remove ${combatant.name}?`)) return; logCombat(encounterId,'DM','remove',`${combatant.name} removed from encounter`); await supabase.from('combatants').delete().eq('id',combatant.id); onUpdate(); }
  async function updatePcResourceFields(updates){ const payload=compactObject(updates); if(!playerState?.id || Object.keys(payload).length===0) return; await supabase.from('player_encounter_state').update(payload).eq('id', playerState.id); onUpdate(); }
  const atTop=idx<=0; const atBottom=idx===-1 || idx>=sorted.length-1;
  return <div className={`initiative-row ${isActive?'active-turn':''} ${isNextUp?'initiative-row--next-up':''}`} style={{display:'block',padding:'8px 12px'}}><div style={{display:'flex',alignItems:'stretch',gap:8}}>{isDM && <div style={{display:'flex',flexDirection:'column',gap:1,justifyContent:'center'}}><button style={{fontSize:9,lineHeight:1,padding:'1px 3px',opacity:atTop?0.15:0.5,cursor:atTop?'default':'pointer',background:'none',border:'none',color:'var(--text-secondary)'}} onClick={e=>{e.stopPropagation(); moveUp();}} disabled={atTop}>▲</button><button style={{fontSize:9,lineHeight:1,padding:'1px 3px',opacity:atBottom?0.15:0.5,cursor:atBottom?'default':'pointer',background:'none',border:'none',color:'var(--text-secondary)'}} onClick={e=>{e.stopPropagation(); moveDown();}} disabled={atBottom}>▼</button></div>}{isDM ? <input className="initiative-number-input" type="number" value={inputValue} onChange={e=>setInputValue(e.target.value)} onFocus={()=>{isFocused.current=true;}} onBlur={()=>{isFocused.current=false; saveInitiative();}} onKeyDown={e=>{ if(e.key==='Enter') e.target.blur(); e.stopPropagation(); }} placeholder="—" /> : <span className="initiative-number">{combatant.initiative_total ?? '—'}</span>}<div style={{flex:1,minWidth:0,display:'flex',flexDirection:'column',gap:6}}><div className="initiative-name-bar"><div className="initiative-name-bar-main">{miniMarker && <span className="initiative-mini-marker">{miniMarker}</span>}<span className="initiative-name-bar-text">{combatant.name}</span></div><div className="initiative-name-bar-badges">{isActive && <span className="display-order-tag display-order-tag--active">Current</span>}{!isActive && isNextUp && <span className="display-order-tag display-order-tag--next">On Deck</span>}<span className={`badge badge-${combatant.side.toLowerCase()}`}>{combatant.side}</span>{(enemyBloodied || pcBloodied) && <span className="badge badge-bloodied">BLOODIED</span>}</div></div><div style={{display:'flex',alignItems:'center',justifyContent:'space-between',gap:8,flexWrap:'wrap'}}>{(classLine || ancestryLine) && <div style={{display:'flex',flexDirection:'column',gap:2,minWidth:0,flex:1}}>{classLine && <span style={{fontSize:10,color:'var(--text-secondary)',overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{classLine}</span>}{ancestryLine && <span style={{fontSize:10,color:'var(--text-muted)',overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{ancestryLine}</span>}</div>}<button className={`reaction-pill ${rxUsed?'reaction-pill--used':'reaction-pill--ready'} ${canToggleReaction?'reaction-pill--clickable':''}`} onClick={e=>{e.stopPropagation(); toggleReaction();}} style={{cursor:canToggleReaction?'pointer':'default'}}>⚡ {rxUsed?'USED':'REACT'}</button></div></div>{isDM && isNonPC && <span className="expand-toggle" style={{alignSelf:'center'}} onClick={()=>setExpanded(e=>!e)}>{expanded?'▲':'▼'}</span>}</div>{(showPcHp || showNonPcHp) && <div style={{marginTop:6,display:'flex',flexDirection:'column',gap:4}}>{isPC && wsActive && wsHpMax != null && <MiniHpBar current={wsHpCurrent} max={wsHpMax} color="var(--accent-green)" label={wsFormName ? `🐻 ${wsFormName}` : '🐻 Beast Form'} />}{showPcHp && <MiniHpBar current={pcHpCurrent} max={pcHpMax} tempHp={tempHp} label={wsActive ? 'Player HP' : null} />}{showNonPcHp && <MiniHpBar current={enemyHpCurrent} max={enemyHpMax} />}{isDisplay && isNPC && showNonPcHp && <div style={{fontSize:11,color:'var(--text-muted)',textAlign:'right'}}>{enemyHpCurrent} / {enemyHpMax} HP</div>}</div>}{isPC && isDM && <div style={{marginTop:4,display:'flex',gap:6,flexWrap:'wrap',alignItems:'center'}}>{passivePerception !== null && <span style={{fontSize:10,color:'var(--text-muted)',background:'var(--bg-panel-3)',border:'1px solid var(--border)',borderRadius:3,padding:'1px 5px'}}>PP {passivePerception}</span>}<button onClick={e=>{e.stopPropagation(); togglePcConcentration();}} style={{fontSize:10,padding:'1px 7px',borderRadius:3,fontWeight:700,cursor:'pointer',border:`1px solid ${concentration?'var(--accent-gold)':'var(--border)'}`,background:concentration?'#3a2e00':'var(--bg-panel-3)',color:concentration?'var(--accent-gold)':'var(--text-muted)'}}>🔮 {concentration?'Concentrating':'Concentration'}</button></div>}{isPC && !isDM && concentration && <div style={{marginTop:4}}><span style={{fontSize:10,color:'var(--accent-gold)',background:'#3a2e00',border:'1px solid var(--accent-gold)',borderRadius:3,padding:'1px 5px'}}>🔮 CON</span></div>}{isPC && playerState && <><PcResourceSummary profile={pcProfile} state={playerState} isDM={isDM} onUpdate={onUpdate} />{pcResources.some(r=>r.id==='celestial-revelation') && <div style={{marginTop:4}}><span className={`badge ${readBooleanField(playerState,['celestial_revelation_used'],false)?'badge-bloodied':''}`} style={{fontSize:10}}>{readBooleanField(playerState,['celestial_revelation_used'],false)?'Celestial Revelation Used':'Celestial Revelation Ready'}</span></div>}</>}{displayConditions.length>0 && <div style={{marginTop:4,display:'flex',flexWrap:'wrap',gap:4}}>{displayConditions.map(code=><span key={code} className="condition-chip" style={{background:CONDITION_COLOURS[code] || 'var(--cond-default)',cursor:isDM?'pointer':'default',userSelect:'none'}} onClick={e=>{ if(!isDM) return; e.stopPropagation(); isPC?togglePcCondition(code):toggleEnemyCondition(code); }}>{code}</span>)}</div>}{isPC && isDM && <div style={{marginTop:4}}><button className="btn btn-ghost" style={{fontSize:11,padding:'2px 8px'}} onClick={e=>{e.stopPropagation(); setCondPickerOpen(p=>!p);}}>{condPickerOpen?'Close Conditions':'+ Conditions'}</button>{condPickerOpen && <div className="condition-picker" style={{marginTop:6}}>{CONDITIONS.map(({code})=><button key={code} className={`condition-picker-btn ${pcConditions.includes(code)?'active':''}`} style={{background:pcConditions.includes(code)?CONDITION_COLOURS[code]:undefined}} onClick={e=>{e.stopPropagation(); togglePcCondition(code);}}>{code}</button>)}</div>}</div>}{hasLegendary && <div style={{marginTop:6,display:'flex',flexDirection:'column',gap:4,paddingLeft:2}}>{laMax>0 && <LegendaryPips label="LA" max={laMax} used={laUsed} isDM={isDM} onSpend={()=>spendLegendary('legendary_actions_used',laMax)} onRestore={()=>restoreLegendary('legendary_actions_used')} onReset={()=>resetLegendary('legendary_actions_used')} isActive={isActive} />}{lrMax>0 && <LegendaryPips label="LR" max={lrMax} used={lrUsed} isDM={isDM} onSpend={()=>spendLegendary('legendary_resistances_used',lrMax)} onRestore={()=>restoreLegendary('legendary_resistances_used')} onReset={()=>resetLegendary('legendary_resistances_used')} isActive={false} />}</div>}{conDc!==null && <div className="con-check-banner con-check-banner--dm" style={{marginTop:6}}><span className="con-check-label">🔮 CON SAVE</span><span className="con-check-dc">DC {conDc}</span></div>}{isPC && isDM && showPcHp && <div style={{marginTop:6}}><DmgHealRow onDamage={applyPcDamage} onHeal={applyPcHeal} compact /></div>}{isDM && isNonPC && expanded && <div className="monster-dm-controls"><div style={{marginBottom:10}}><DmgHealRow onDamage={applyEnemyDamage} onHeal={applyEnemyHeal} compact /><div style={{marginTop:4,fontSize:11,color:'var(--text-muted)',textAlign:'center'}}>{combatant.hp_current} / {combatant.hp_max} HP</div></div>{(classLine || miniMarker) && <div style={{marginBottom:10,display:'flex',flexWrap:'wrap',gap:6}}>{classLine && <span style={{fontSize:11,color:'var(--text-secondary)',background:'var(--bg-panel-3)',border:'1px solid var(--border)',borderRadius:4,padding:'3px 7px'}}>{classLine}</span>}{miniMarker && <span style={{fontSize:11,color:'var(--accent-blue)',background:'rgba(74,158,255,0.10)',border:'1px solid var(--accent-blue)',borderRadius:4,padding:'3px 7px',fontWeight:700}}>Mini {miniMarker}</span>}</div>}<EnemySlotGrid combatant={combatant} onUpdate={onUpdate} />{hasAnyMod && <div style={{display:'flex',gap:4,flexWrap:'wrap',marginBottom:8}}>{MODS.map(m=>{ const val=combatant[`mod_${m}`] ?? 0; return <div key={m} style={{display:'flex',flexDirection:'column',alignItems:'center',background:'var(--bg-panel-3)',border:'1px solid var(--border)',borderRadius:4,padding:'2px 5px',minWidth:30}}><span style={{fontSize:9,color:'var(--text-muted)',textTransform:'uppercase'}}>{m}</span><span style={{fontSize:11,fontWeight:700}}>{val>=0?'+':''}{val}</span></div>; })}</div>}<div style={{marginBottom:8}}><button className="btn btn-ghost" style={{fontSize:11,padding:'2px 8px',marginBottom:4}} onClick={()=>setResPicker(p=>!p)}>{resPicker ? 'Close' : `Resist/Immune ${resistances.length+immunities.length>0?`(${resistances.length+immunities.length})`:''}`}</button>{resPicker && <div><div style={{fontSize:10,color:'var(--text-muted)',fontWeight:700,textTransform:'uppercase',marginBottom:4}}>Resistances</div><div style={{display:'flex',flexWrap:'wrap',gap:3,marginBottom:8}}>{DAMAGE_TYPES.map(t=><button key={t} style={{fontSize:10,padding:'2px 6px',borderRadius:3,cursor:'pointer',border:`1px solid ${resistances.includes(t)?'var(--accent-blue)':'var(--border)'}`,background:resistances.includes(t)?'rgba(74,158,255,0.2)':'var(--bg-panel-3)',color:resistances.includes(t)?'var(--accent-blue)':'var(--text-secondary)'}} onClick={()=>toggleDamageType('resistances',t)}>{t}</button>)}</div><div style={{fontSize:10,color:'var(--text-muted)',fontWeight:700,textTransform:'uppercase',marginBottom:4}}>Immunities</div><div style={{display:'flex',flexWrap:'wrap',gap:3}}>{DAMAGE_TYPES.map(t=><button key={t} style={{fontSize:10,padding:'2px 6px',borderRadius:3,cursor:'pointer',border:`1px solid ${immunities.includes(t)?'var(--accent-gold)':'var(--border)'}`,background:immunities.includes(t)?'rgba(240,180,41,0.15)':'var(--bg-panel-3)',color:immunities.includes(t)?'var(--accent-gold)':'var(--text-secondary)'}} onClick={()=>toggleDamageType('immunities',t)}>{t}</button>)}</div></div>}</div><div style={{marginBottom:8}}><button className="btn btn-ghost" style={{fontSize:12,padding:'2px 8px'}} onClick={()=>setCondPickerOpen(p=>!p)}>{condPickerOpen ? 'Close Conditions' : '+ Conditions'}</button>{condPickerOpen && <div className="condition-picker" style={{marginTop:6}}>{CONDITIONS.map(({code})=><button key={code} className={`condition-picker-btn ${conditions.includes(code)?'active':''}`} style={{background:conditions.includes(code)?CONDITION_COLOURS[code]:undefined}} onClick={()=>toggleEnemyCondition(code)}>{code}</button>)}</div>}</div>{combatant.notes && <div className="monster-notes" style={{marginBottom:8}}>{combatant.notes}</div>}<button className="btn btn-danger" onClick={removeCombatant}>Remove</button></div>}</div>;
}

function AddCombatantModal({ encounterId, onUpdate, onClose }) {
  const [mode, setMode] = useState('template'); const [templates, setTemplates] = useState([]); const [templateFilter, setTemplateFilter] = useState('ALL'); const [adding, setAdding] = useState(false); const [name,setName]=useState(''); const [ac,setAc]=useState(10); const [hp,setHp]=useState(10); const [mod,setMod]=useState(0); const [side,setSide]=useState('ENEMY'); const [className,setClassName]=useState(''); const [subclassName,setSubclassName]=useState(''); const [classLevel,setClassLevel]=useState(''); const [className2,setClassName2]=useState(''); const [subclassName2,setSubclassName2]=useState(''); const [classLevel2,setClassLevel2]=useState(''); const [miniMarker,setMiniMarker]=useState(''); const [saving,setSaving]=useState(false);
  useEffect(()=>{ if(mode==='template') supabase.from('profiles_monsters').select('*').order('name').then(({data})=>setTemplates(data || [])); }, [mode]);
  useEffect(()=>{ const previousOverflow=document.body.style.overflow; document.body.style.overflow='hidden'; function handleKeyDown(e){ if(e.key==='Escape') onClose(); } window.addEventListener('keydown', handleKeyDown); return ()=>{ document.body.style.overflow=previousOverflow; window.removeEventListener('keydown', handleKeyDown); }; }, [onClose]);
  async function addFromTemplate(template){ if(adding) return; setAdding(true); const basePayload={ encounter_id:encounterId,name:template.name,side:template.side || 'ENEMY',ac:template.ac,hp_max:template.hp_max,hp_current:template.hp_max,initiative_mod:template.initiative_mod || 0,notes:template.notes || null,mod_str:template.mod_str || 0,mod_dex:template.mod_dex || 0,mod_con:template.mod_con || 0,mod_int:template.mod_int || 0,mod_wis:template.mod_wis || 0,mod_cha:template.mod_cha || 0,resistances:template.resistances || [],immunities:template.immunities || [],legendary_actions_max:template.legendary_actions_max || 0,legendary_resistances_max:template.legendary_resistances_max || 0,slots_max_1:template.slots_max_1 || 0,slots_max_2:template.slots_max_2 || 0,slots_max_3:template.slots_max_3 || 0,slots_max_4:template.slots_max_4 || 0,slots_max_5:template.slots_max_5 || 0,slots_max_6:template.slots_max_6 || 0,slots_max_7:template.slots_max_7 || 0,slots_max_8:template.slots_max_8 || 0,slots_max_9:template.slots_max_9 || 0 }; const enrichedPayload=compactObject({...basePayload,class_name:template.class_name || null,subclass_name:template.subclass_name || null,class_level:template.class_level != null ? readNumberField(template,['class_level'],1) : null,class_name_2:template.class_name_2 || null,subclass_name_2:template.subclass_name_2 || null,class_level_2:template.class_level_2 != null ? readNumberField(template,['class_level_2'],0) : null,mini_marker:template.mini_marker || null}); const attempt=await supabase.from('combatants').insert(enrichedPayload); if(attempt.error){ await supabase.from('combatants').insert(basePayload); } setAdding(false); onUpdate(); onClose(); }
  async function addManual(){ if(!name.trim() || saving) return; setSaving(true); const basePayload={ encounter_id:encounterId,name:name.trim(),side,ac:parseInt(ac,10)||10,hp_max:parseInt(hp,10)||10,hp_current:parseInt(hp,10)||10,initiative_mod:parseInt(mod,10)||0 }; const enrichedPayload=compactObject({...basePayload,class_name:className.trim() || null,subclass_name:subclassName.trim() || null,class_level:classLevel !== '' ? parseInt(classLevel,10)||1 : null,class_name_2:className2.trim() || null,subclass_name_2:subclassName2.trim() || null,class_level_2:classLevel2 !== '' ? parseInt(classLevel2,10)||0 : null,mini_marker:miniMarker.trim() || null}); const attempt=await supabase.from('combatants').insert(enrichedPayload); if(attempt.error){ await supabase.from('combatants').insert(basePayload); } setSaving(false); onUpdate(); onClose(); }
  const filteredTemplates=templateFilter==='ALL'?templates:templates.filter(t=>t.side===templateFilter);
  return <div className="modal-overlay" onClick={onClose}><div className="modal-panel add-combatant-modal" onClick={e=>e.stopPropagation()}><div className="modal-header"><div><div className="panel-title" style={{marginBottom:4}}>Add Combatant</div><div className="modal-subtitle">Add from template or enter a manual combatant.</div></div><button className="btn btn-ghost btn-icon" onClick={onClose}>✕</button></div><div style={{display:'flex',gap:8}}><button className="btn btn-ghost" style={{flex:1,borderColor:mode==='template'?'var(--accent-blue)':'var(--border)',color:mode==='template'?'var(--accent-blue)':'var(--text-secondary)'}} onClick={()=>setMode('template')}>From Template</button><button className="btn btn-ghost" style={{flex:1,borderColor:mode==='manual'?'var(--accent-blue)':'var(--border)',color:mode==='manual'?'var(--accent-blue)':'var(--text-secondary)'}} onClick={()=>setMode('manual')}>Manual</button></div>{mode==='template' && <><div style={{display:'flex',gap:6,flexWrap:'wrap'}}>{['ALL','ENEMY','NPC'].map(f=><button key={f} className="btn btn-ghost" style={{fontSize:11,padding:'2px 8px',borderColor:templateFilter===f?'var(--accent-blue)':'var(--border)',color:templateFilter===f?'var(--accent-blue)':'var(--text-secondary)'}} onClick={()=>setTemplateFilter(f)}>{f}</button>)}</div>{templates.length===0 && <div className="empty-state">No templates yet. Add them in Manage.</div>}<div className="add-combatant-template-list">{filteredTemplates.map(t=><div key={t.id} className="add-combatant-template-row"><div style={{minWidth:0,display:'flex',flexDirection:'column',gap:2}}><div style={{minWidth:0}}><span className={`badge badge-${(t.side || 'enemy').toLowerCase()}`} style={{marginRight:6}}>{t.side || 'ENEMY'}</span>{t.mini_marker && <span style={{marginRight:6,color:'var(--accent-blue)',fontSize:11,fontWeight:700}}>[{t.mini_marker}]</span>}<span style={{fontSize:13,fontWeight:600}}>{t.name}</span><span style={{fontSize:11,color:'var(--text-muted)',marginLeft:6}}>AC {t.ac} HP {t.hp_max}</span>{(t.legendary_actions_max > 0 || t.legendary_resistances_max > 0) && <span style={{fontSize:10,color:'var(--accent-gold)',marginLeft:6}}>★ Legendary</span>}</div>{formatClassLine(t) && <div style={{fontSize:11,color:'var(--text-muted)'}}>{formatClassLine(t)}</div>}</div><button className="btn btn-primary btn-icon" onClick={()=>addFromTemplate(t)} disabled={adding} style={{fontSize:16,minWidth:32,minHeight:32}}>+</button></div>)}</div></>}{mode==='manual' && <><input className="form-input" placeholder="Name" value={name} onChange={e=>setName(e.target.value)} /><div className="form-row add-combatant-manual-grid"><div className="form-group"><label className="form-label">Side</label><select className="form-input" value={side} onChange={e=>setSide(e.target.value)}><option value="ENEMY">Enemy</option><option value="NPC">NPC</option><option value="PC">PC</option></select></div><div className="form-group"><label className="form-label">AC</label><input className="form-input" type="number" value={ac} onChange={e=>setAc(e.target.value)} /></div><div className="form-group"><label className="form-label">HP</label><input className="form-input" type="number" value={hp} onChange={e=>setHp(e.target.value)} /></div><div className="form-group"><label className="form-label">Init Mod</label><input className="form-input" type="number" value={mod} onChange={e=>setMod(e.target.value)} /></div></div><div className="form-row add-combatant-manual-grid"><div className="form-group"><label className="form-label">Primary Class</label><input className="form-input" value={className} onChange={e=>setClassName(e.target.value)} placeholder="e.g. Fighter" /></div><div className="form-group"><label className="form-label">Primary Subclass</label><input className="form-input" value={subclassName} onChange={e=>setSubclassName(e.target.value)} placeholder="Optional" /></div><div className="form-group"><label className="form-label">Primary Level</label><input className="form-input" type="number" value={classLevel} onChange={e=>setClassLevel(e.target.value)} placeholder="Optional" /></div><div className="form-group"><label className="form-label">Mini Marker</label><input className="form-input" value={miniMarker} onChange={e=>setMiniMarker(e.target.value)} placeholder="A / 1 / 🔴" maxLength={8} /></div></div><div className="form-row add-combatant-manual-grid"><div className="form-group"><label className="form-label">Secondary Class</label><input className="form-input" value={className2} onChange={e=>setClassName2(e.target.value)} placeholder="Optional" /></div><div className="form-group"><label className="form-label">Secondary Subclass</label><input className="form-input" value={subclassName2} onChange={e=>setSubclassName2(e.target.value)} placeholder="Optional" /></div><div className="form-group"><label className="form-label">Secondary Level</label><input className="form-input" type="number" value={classLevel2} onChange={e=>setClassLevel2(e.target.value)} placeholder="Optional" /></div></div><div className="form-row" style={{flexWrap:'wrap'}}><button className="btn btn-primary" onClick={addManual} disabled={saving || !name.trim()}>{saving?'Adding…':'Add'}</button><button className="btn btn-ghost" onClick={onClose}>Cancel</button></div></>}</div></div>;
}
