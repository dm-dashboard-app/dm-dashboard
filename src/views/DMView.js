import React,{useState,useEffect,useCallback} from 'react';
import { supabase,signOut } from '../supabaseClient';
import usePolling from '../hooks/usePolling';
import InitiativePanel from '../components/InitiativePanelNext';
import SecretRollInbox from '../components/SecretRollInbox';
import EncounterSetup from '../components/EncounterSetup';
import ManagementScreens from '../components/ManagementScreens';
import ShortRestModal from '../components/ShortRestModal';
import DMNotificationOverlay from '../components/DMNotificationOverlay';
import LongRestPrepModal from '../components/LongRestPrepModal';
import { readNumberField } from '../utils/classResources';
import { getLongRestResourcePatch } from '../utils/resourcePolicy';
import { hasPreparationRequirement } from '../utils/spellWorkflow';
import { flattenStates } from './dm/dmViewUtils';
import RecentAlertsStrip from './dm/RecentAlertsStrip';
import DMCombatLog from './dm/DMCombatLog';
import DMPlayerCardsSection from './dm/DMPlayerCardsSection';
function buildLongRestStatePatch(state={}){const profile=state?.profiles_players||{};const patch={...getLongRestResourcePatch(state,profile)};const maxHpOverride=readNumberField(state,['max_hp_override'],null);const profileMax=readNumberField(profile,['max_hp'],0);const maxHp=maxHpOverride!==null?maxHpOverride:profileMax;if(maxHp>0&&Object.prototype.hasOwnProperty.call(state,'current_hp')) patch.current_hp=maxHp;if(Object.prototype.hasOwnProperty.call(state,'conditions')) patch.conditions=(state.conditions||[]).filter(code=>code!=='UNC'&&code!=='PRN');if(Object.prototype.hasOwnProperty.call(state,'concentration')) patch.concentration=false;if(Object.prototype.hasOwnProperty.call(state,'concentration_check_dc')) patch.concentration_check_dc=null;if(Object.prototype.hasOwnProperty.call(state,'concentration_spell_id')) patch.concentration_spell_id=null;if(Object.prototype.hasOwnProperty.call(state,'wildshape_active')) patch.wildshape_active=false;if(Object.prototype.hasOwnProperty.call(state,'wildshape_form_id')) patch.wildshape_form_id=null;if(Object.prototype.hasOwnProperty.call(state,'wildshape_hp_current')) patch.wildshape_hp_current=null;if(Object.prototype.hasOwnProperty.call(state,'spell_prep_ready')) patch.spell_prep_ready=false;for(let level=1;level<=9;level+=1) if(Object.prototype.hasOwnProperty.call(state,`slots_used_${level}`)) patch[`slots_used_${level}`]=0;return patch;}
const tabsRowStyle={width:'min(1180px, calc(100vw - 24px))',margin:'0 auto',display:'grid',gridTemplateColumns:'repeat(4, minmax(0, 1fr))',gap:8,padding:4,borderRadius:16,background:'rgba(17,22,29,.96)',border:'1px solid rgba(74,158,255,.12)',boxShadow:'0 12px 24px rgba(0,0,0,.24)'};const tabsButtonBase={minHeight:42,display:'inline-flex',alignItems:'center',justifyContent:'center',gap:6,padding:'8px 10px',borderRadius:12,border:'1px solid transparent',background:'transparent',color:'var(--text-secondary)',fontSize:13,fontWeight:700,appearance:'none',WebkitAppearance:'none',WebkitTapHighlightColor:'transparent',outline:'none',boxShadow:'none'};
const dockShellStyle={position:'fixed',left:0,right:0,bottom:0,zIndex:131,padding:'0 12px calc(10px + env(safe-area-inset-bottom,0px))',background:'linear-gradient(180deg, rgba(17,22,29,0), rgba(17,22,29,.72) 18%, rgba(17,22,29,.92) 42%, rgba(17,22,29,.98))',pointerEvents:'none'};const dockInnerStyle={width:'min(1180px, calc(100vw - 24px))',margin:'0 auto',display:'flex',flexDirection:'column',gap:10,pointerEvents:'auto'};
export default function DMView(){const[encounter,setEncounter]=useState(null);const[combatants,setCombatants]=useState([]);const[playerStates,setPlayerStates]=useState([]);const[tab,setTab]=useState('combat');const[loading,setLoading]=useState(true);const[displayToken,setDisplayToken]=useState(null);const[joinCodes,setJoinCodes]=useState([]);const[encounterId,setEncounterId]=useState(null);const[rollingInit,setRollingInit]=useState(false);const[shortRestOpen,setShortRestOpen]=useState(false);const[longRestPrepOpen,setLongRestPrepOpen]=useState(false);const[recentRollCount,setRecentRollCount]=useState(0);const[recentAlertCount,setRecentAlertCount]=useState(0);useEffect(()=>{loadLatestEncounter();},[]);useEffect(()=>{setLongRestPrepOpen(!!encounter?.long_rest_prep_active);},[encounter?.long_rest_prep_active]);async function loadLatestEncounter(){setLoading(true);const{data}=await supabase.from('encounters').select('*').order('created_at',{ascending:false}).limit(1).maybeSingle();if(data){setEncounter(data);setEncounterId(data.id);}setLoading(false);}const refreshAll=useCallback(async()=>{if(!encounterId) return;const[enc,comb,states,token,codes]=await Promise.all([supabase.from('encounters').select('*').eq('id',encounterId).maybeSingle(),supabase.from('combatants').select('*').eq('encounter_id',encounterId).order('initiative_total',{ascending:false,nullsFirst:false}),supabase.from('player_encounter_state').select('*, profiles_players(*), profiles_wildshape(form_name, hp_max)').eq('encounter_id',encounterId),supabase.from('display_sessions').select('token').eq('encounter_id',encounterId).maybeSingle(),supabase.from('player_sessions').select('join_code, profiles_players(name)').eq('encounter_id',encounterId)]);if(enc.data) setEncounter(enc.data);setCombatants(comb.data||[]);setPlayerStates(flattenStates(states.data));setDisplayToken(token.data?.token||null);setJoinCodes(codes.data||[]);},[encounterId]);const refreshActivityPresence=useCallback(async()=>{if(!encounterId) return;const[rolls,alerts]=await Promise.all([supabase.from('secret_rolls').select('id',{count:'exact',head:true}).eq('encounter_id',encounterId),supabase.from('concentration_checks').select('id',{count:'exact',head:true}).eq('encounter_id',encounterId).eq('result','pending')]);setRecentRollCount(rolls.count||0);setRecentAlertCount(alerts.count||0);},[encounterId]);usePolling(refreshAll,2000,!!encounterId);usePolling(refreshActivityPresence,3000,!!encounterId);useEffect(()=>{if(encounterId){refreshAll();refreshActivityPresence();}},[encounterId,refreshAll,refreshActivityPresence]);async function handleNextTurn(){if(!encounter) return;await supabase.rpc('advance_turn',{ p_encounter_id:encounter.id });refreshAll();}async function handleGenerateDisplayToken(){if(!encounter) return;await supabase.rpc('generate_display_token',{ p_encounter_id:encounter.id });refreshAll();}async function handleRevokeDisplayToken(){if(!encounter) return;await supabase.from('display_sessions').delete().eq('encounter_id',encounter.id);setDisplayToken(null);}async function executeLongRest(){if(!encounter) return;const{data:latestStates,error:latestStatesError}=await supabase.from('player_encounter_state').select('*, profiles_players(*), profiles_wildshape(form_name, hp_max)').eq('encounter_id',encounter.id);if(latestStatesError) return;const states=flattenStates(latestStates||[]);const updateResults=await Promise.all(states.map(async state=>supabase.from('player_encounter_state').update(buildLongRestStatePatch(state)).eq('id',state.id)));const failedUpdate=updateResults.find(result=>result.error);if(failedUpdate?.error) return;await supabase.from('concentration_checks').update({ result:'cleared' }).eq('encounter_id',encounter.id).eq('result','pending');await supabase.from('encounters').update({ round:1,turn_index:0,long_rest_prep_active:false }).eq('id',encounter.id);await supabase.from('combat_log').insert({ encounter_id:encounter.id,actor:'DM',action:'rest',detail:'Long Rest completed — HP, spell slots, concentration, and Wild Shape reset' });setLongRestPrepOpen(false);await refreshAll();await refreshActivityPresence();}async function beginLongRest(){if(!encounter) return;if(!window.confirm('Long Rest — begin spell preparation and then restore all player HP, spell slots, and wild shape uses?')) return;const{data:latestStates,error:latestStatesError}=await supabase.from('player_encounter_state').select('*, profiles_players(*), profiles_wildshape(form_name, hp_max)').eq('encounter_id',encounter.id);if(latestStatesError) return;const states=flattenStates(latestStates||[]);const affected=states.filter(state=>hasPreparationRequirement(state?.profiles_players||{}));if(affected.length===0){await executeLongRest();return;}await Promise.all(affected.map(async state=>supabase.from('player_encounter_state').update({ spell_prep_ready:false }).eq('id',state.id)));await supabase.from('encounters').update({ long_rest_prep_active:true }).eq('id',encounter.id);setLongRestPrepOpen(true);await refreshAll();}async function cancelLongRestPrep(){if(!encounter) return;const targets=playerStates.filter(state=>Object.prototype.hasOwnProperty.call(state||{},'spell_prep_ready'));await Promise.all(targets.map(async state=>supabase.from('player_encounter_state').update({ spell_prep_ready:false }).eq('id',state.id)));await supabase.from('encounters').update({ long_rest_prep_active:false }).eq('id',encounter.id);setLongRestPrepOpen(false);await refreshAll();}async function handleRollEnemyInitiative(){if(!encounter) return;const targets=combatants.filter(c=>c.side!=='PC'&&c.initiative_total==null);if(targets.length===0){const reroll=combatants.filter(c=>c.side!=='PC');if(!window.confirm('All enemies already have initiative. Reroll everyone and reset round to 1?')) return;setRollingInit(true);await Promise.all(reroll.map(c=>{const roll=Math.floor(Math.random()*20)+1+(c.initiative_mod??0);return supabase.rpc('set_initiative',{ p_combatant_id:c.id,p_total:roll });}));await supabase.from('encounters').update({ round:1,turn_index:0 }).eq('id',encounter.id);setRollingInit(false);refreshAll();return;}setRollingInit(true);await Promise.all(targets.map(c=>{const roll=Math.floor(Math.random()*20)+1+(c.initiative_mod??0);return supabase.rpc('set_initiative',{ p_combatant_id:c.id,p_total:roll });}));await supabase.from('encounters').update({ round:1,turn_index:0 }).eq('id',encounter.id);setRollingInit(false);refreshAll();}function handleFrontScreen(){setEncounter(null);setEncounterId(null);setCombatants([]);setPlayerStates([]);setDisplayToken(null);setJoinCodes([]);setTab('manage');}function handleTabSwitch(nextTab,event){setTab(nextTab);event?.currentTarget?.blur();}if(loading) return <div className="splash"><div className="splash-text">Loading…</div></div>;if(!encounter) return <div className="app-shell"><div className="main-content dm-main-content" style={{ paddingTop:12 }}><EncounterSetup onEncounterCreated={enc=>{setEncounter(enc);setEncounterId(enc.id);setTab('combat');}} /><ManagementScreens currentEncounter={null} displayToken={null} joinCodes={[]} onGenerateDisplayToken={null} onRevokeDisplayToken={null} onFrontScreen={null} onSignOut={signOut} /></div></div>;const pcCombatants=combatants.filter(c=>c.side==='PC');const pendingAlertCount=playerStates.filter(s=>s.concentration_check_dc!=null).length;const activityCount=recentRollCount+recentAlertCount+pendingAlertCount;function tabButtonStyle(active){return active?{...tabsButtonBase,color:'var(--accent-blue)',background:'rgba(74,158,255,.12)',borderColor:'rgba(74,158,255,.28)'}:{...tabsButtonBase};}return <div className="app-shell"><div className="main-content dm-main-content main-content--with-bottom-dock" style={{ paddingTop:12,paddingBottom:238 }}>{tab==='combat'&&<div className="dm-combat-layout"><div className="dm-initiative-column"><div className="initiative-top-bar"><div className="initiative-top-bar-primary">Round {encounter.round}</div></div><InitiativePanel encounter={encounter} combatants={combatants} playerStates={playerStates} role="dm" onUpdate={refreshAll} /></div></div>}{tab==='players'&&<DMPlayerCardsSection combatants={pcCombatants} playerStates={playerStates} encounterId={encounter.id} playerEditMode={encounter.player_edit_mode} onUpdate={refreshAll} title="Players" subtitle={`${pcCombatants.length} player${pcCombatants.length===1?'':'s'} in encounter`} />}{tab==='activity'&&<div className="dm-activity-layout"><div className="dm-activity-primary"><RecentAlertsStrip encounterId={encounter.id} mode="panel" /><SecretRollInbox encounterId={encounter.id} /></div><div className="dm-activity-secondary"><DMCombatLog encounterId={encounter.id} /></div></div>}{tab==='manage'&&<ManagementScreens onEncounterCreated={enc=>{setEncounter(enc);setEncounterId(enc.id);setTab('combat');}} currentEncounter={encounter} displayToken={displayToken} joinCodes={joinCodes} onGenerateDisplayToken={handleGenerateDisplayToken} onRevokeDisplayToken={handleRevokeDisplayToken} onFrontScreen={handleFrontScreen} onSignOut={signOut} />}</div><div style={dockShellStyle}><div style={dockInnerStyle}><DMNotificationOverlay encounterId={encounter.id} inline /><div style={tabsRowStyle}><button type="button" className="dm-bottom-tab-btn" style={tabButtonStyle(tab==='combat')} onClick={event=>handleTabSwitch('combat',event)} onFocus={event=>event.currentTarget.blur()}>⚔ Combat</button><button type="button" className="dm-bottom-tab-btn" style={tabButtonStyle(tab==='players')} onClick={event=>handleTabSwitch('players',event)} onFocus={event=>event.currentTarget.blur()}>🧙 Players</button><button type="button" className="dm-bottom-tab-btn" style={tabButtonStyle(tab==='activity')} onClick={event=>handleTabSwitch('activity',event)} onFocus={event=>event.currentTarget.blur()}>📋 Activity{activityCount>0?<span className="tab-badge">{activityCount}</span>:''}</button><button type="button" className="dm-bottom-tab-btn" style={tabButtonStyle(tab==='manage')} onClick={event=>handleTabSwitch('manage',event)} onFocus={event=>event.currentTarget.blur()}>⚙ Manage</button></div><div className="dm-bottom-action-row"><button className="btn btn-ghost" onClick={beginLongRest}>Long Rest</button><button className="btn btn-ghost" onClick={()=>setShortRestOpen(true)}>Short Rest</button><button className="btn btn-ghost" onClick={handleRollEnemyInitiative} disabled={rollingInit}>{rollingInit?'Rolling…':'Initiative'}</button><button className="btn btn-primary dm-bottom-action-next" onClick={handleNextTurn}>▶ Next</button></div></div></div><ShortRestModal open={shortRestOpen} playerStates={playerStates} encounterId={encounter.id} onClose={()=>setShortRestOpen(false)} onComplete={refreshAll} /><LongRestPrepModal open={longRestPrepOpen} playerStates={playerStates} onClose={cancelLongRestPrep} onComplete={executeLongRest} onRefresh={refreshAll} /></div>;}
